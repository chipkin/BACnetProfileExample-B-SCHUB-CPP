# Build the B-SCHUB example and publish a GitHub Release (binaries + metrics.json) when a
# vX.Y.Z tag is pushed. PRs run the same build + smoke test without
# publishing. The build is exactly what README.md tells a customer to run: plain CMake in
# the adapter's default SOURCE mode, which compiles the stack into the executable.
#
# The CAS BACnet Stack is a PRIVATE git submodule: the repository secret CAS_STACK_PAT must
# hold a PAT with read access to chipkin/cas-bacnet-stack.
# SC-TRANSPORT:BEGIN
#
# This workflow legitimately differs from the series template
# (../BACnetProfileExample-B-SS-CPP/.github/workflows/release.yml) beyond example names:
# this repository is the only one in the series with a BACnet/SC WebSocket+TLS transport
# and its vcpkg dependency (libwebsockets, OpenSSL). The vcpkg-caching steps below (adapted
# from ../plugfest-example/.github/workflows/build.yml) and the BACnet/SC test step have no
# equivalent in the template and are NOT a drift to reconcile - each such block below is
# marked between its own "SC-TRANSPORT:BEGIN"/"SC-TRANSPORT:END" comment lines; see
# ../tools/check-series.sh check 11's B-SCHUB exemption (it diffs only outside those marked
# blocks) and docs/bacnet-sc-transport-plan.md's "CI" section for why.
# SC-TRANSPORT:END
name: build-and-release

on:
  pull_request:
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:

permissions:
  contents: write
  # SC-TRANSPORT:BEGIN - Windows code signing: azure/login uses GitHub OIDC, which needs
  # an ID token. Only the tag build of the Windows leg requests one (see "Code signing").
  id-token: write
  # SC-TRANSPORT:END
  packages: write  # the vcpkg NuGet binary cache on GitHub Packages (issue #17)

jobs:
  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    # SC-TRANSPORT:BEGIN - code signing. The Windows leg of a vX.Y.Z tag build runs in the
    # "release" environment, which the Azure federated credential trusts (subject
    # repo:chipkin/BACnetProfileExample-B-SCHUB-CPP:environment:release). Every other
    # run (PRs, Linux) gets no environment and never signs.
    environment: ${{ (startsWith(github.ref, 'refs/tags/v') && matrix.os == 'windows-2022') && 'release' || '' }}
    # SC-TRANSPORT:END
    strategy:
      fail-fast: false
      matrix:
        include:
          # windows-2022 pins the VS 2022 (v143) toolset the released binaries are built
          # with, so the published metrics stay comparable release to release.
          - os: windows-2022
            artifact: BACnetExampleBSCHUB-windows-x64
            exe: build/Release/BACnetExampleBSCHUB.exe
            metrics: metrics-windows.json
          - os: ubuntu-latest
            artifact: BACnetExampleBSCHUB-linux-x64
            exe: build/BACnetExampleBSCHUB
            metrics: metrics-linux.json
    steps:
      # SC-TRANSPORT:BEGIN - Code signing, part 1 (Windows, tag builds only). Same pattern
      # as chipkin-modbus-explorer: Azure Artifact Signing through GitHub OIDC, no
      # certificate or password stored in GitHub. These steps run FIRST so a signing
      # misconfiguration (missing variable, federated credential or role) fails the run in
      # seconds, before checkout and the long build. A tag build that can't sign fails -
      # no unsigned releases. Part 2 (the signing itself) runs after "Build".
      - name: Validate signing configuration inputs exist
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        shell: pwsh
        env:
          AS_ENDPOINT: ${{ vars.AS_ENDPOINT }}
          AS_ACCOUNT: ${{ vars.AS_SIGNING_ACCOUNT_NAME }}
          AS_PROFILE: ${{ vars.AS_CERTIFICATE_PROFILE_NAME }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:AS_ENDPOINT)) { throw "Missing repo variable AS_ENDPOINT" }
          if ([string]::IsNullOrWhiteSpace($env:AS_ACCOUNT)) { throw "Missing repo variable AS_SIGNING_ACCOUNT_NAME" }
          if ([string]::IsNullOrWhiteSpace($env:AS_PROFILE)) { throw "Missing repo variable AS_CERTIFICATE_PROFILE_NAME" }
          Write-Host "Signing endpoint: $env:AS_ENDPOINT"
          Write-Host "Signing account:  $env:AS_ACCOUNT"
          Write-Host "Cert profile:     $env:AS_PROFILE"

      - name: Azure login (OIDC)
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        uses: azure/login@8216e11d8cd9b42fe925c852af8e76311ff067ac # v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: true

      - name: Check a code-signing token can be issued
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        shell: pwsh
        run: |
          $null = az account get-access-token --resource https://codesigning.azure.net --query expiresOn -o tsv
          if ($LASTEXITCODE -ne 0) { throw "Could not get an Azure Artifact Signing token for this identity" }
          Write-Host "Azure Artifact Signing token OK"
      # SC-TRANSPORT:END
      - name: Enable git long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Checkout (with the CAS BACnet Stack submodule)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.CAS_STACK_PAT || github.token }}

      - name: Stack commit (recorded in metrics.json)
        id: stack
        shell: bash
        run: echo "sha=$(git -C submodules/cas-bacnet-stack rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      # SC-TRANSPORT:BEGIN - vcpkg binary cache for libwebsockets/OpenSSL, adapted from
      # ../plugfest-example/.github/workflows/build.yml (same manifest-mode pattern: point
      # at the runner's own vcpkg via VCPKG_ROOT, cache what it builds under a
      # workspace-local directory, and pre-fetch the manifest's builtin-baseline commit if
      # the runner's bundled vcpkg clone predates it).
      - name: vcpkg binary cache
        uses: actions/cache@v4
        with:
          path: vcpkg-binary-cache
          key: vcpkg-${{ matrix.os }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: vcpkg-${{ matrix.os }}-

      - name: Point at the runner's vcpkg
        shell: bash
        run: |
          echo "VCPKG_ROOT=$VCPKG_INSTALLATION_ROOT" >> "$GITHUB_ENV"
          mkdir -p vcpkg-binary-cache
          echo "VCPKG_BINARY_SOURCES=clear;files,${GITHUB_WORKSPACE}/vcpkg-binary-cache,readwrite" >> "$GITHUB_ENV"

      # A binary cache that doesn't expire (issue #17): actions/cache above is
      # evicted after 7 days unused, and this workflow only runs on pull requests
      # and tags, so a cold OpenSSL/libwebsockets build was the usual case. This
      # adds this repository owner's GitHub Packages NuGet feed as a second
      # vcpkg binary source, using the workflow's own GITHUB_TOKEN (packages:
      # write, above). Pull requests read from it; pushes (tags) and manual
      # runs also write to it. If the feed can't be set up - a pull request from
      # a fork, whose token can't read this owner's packages - the step is
      # skipped and the files cache above still works.
      - name: vcpkg binary cache on GitHub Packages (NuGet)
        continue-on-error: true
        shell: bash
        env:
          FEED_URL: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
          FEED_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FEED_MODE: ${{ github.event_name == 'pull_request' && 'read' || 'readwrite' }}
        run: |
          set -e
          nuget=$("$VCPKG_ROOT/vcpkg" fetch nuget | tail -n 1)
          run_nuget() {
            if [ "${{ runner.os }}" = "Windows" ]; then "$nuget" "$@"; else mono "$nuget" "$@"; fi
          }
          if [ "${{ runner.os }}" != "Windows" ] && ! command -v mono > /dev/null; then
            sudo apt-get update -qq && sudo apt-get install -y -qq mono-complete > /dev/null
          fi
          run_nuget sources add -Source "$FEED_URL" -StorePasswordInClearText -Name GitHubPackages \
            -UserName "${{ github.repository_owner }}" -Password "$FEED_TOKEN"
          run_nuget setapikey "$FEED_TOKEN" -Source "$FEED_URL"
          echo "VCPKG_BINARY_SOURCES=clear;files,${GITHUB_WORKSPACE}/vcpkg-binary-cache,readwrite;nuget,${FEED_URL},${FEED_MODE}" >> "$GITHUB_ENV"
          echo "vcpkg binary cache: files + GitHub Packages NuGet (${FEED_MODE})"
          # x64-windows-static matches this repo's own CMakeLists.txt Windows default (kept in
          # sync with the CAS BACnet Stack adapter's static-runtime default); x64-linux is
          # vcpkg's own default triplet there, so nothing needs to be forced on Linux.
          if [ "${{ runner.os }}" = "Windows" ]; then
            echo "VCPKG_TARGET_TRIPLET=x64-windows-static" >> "$GITHUB_ENV"
          fi

      # The runner image's vcpkg clone is usually older than the manifest's builtin-baseline,
      # and vcpkg then fails with "failed to git show versions/baseline.json". Fetch exactly
      # that commit (read from vcpkg.json so the two cannot drift) when it is missing.
      - name: Make sure the runner vcpkg has the manifest baseline
        shell: bash
        run: |
          baseline=$(python -c "import json;print(json.load(open('vcpkg.json'))['builtin-baseline'])")
          echo "builtin-baseline: $baseline"
          if ! git -C "$VCPKG_ROOT" cat-file -e "${baseline}^{commit}" 2>/dev/null; then
            git -C "$VCPKG_ROOT" fetch --depth=1 origin "$baseline"
          fi
      # SC-TRANSPORT:END
      - name: Configure
        run: cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

      # The README documents `cmake -B build -S .` with no link-mode flag, so assert the
      # adapter's default really is SOURCE. If that default ever changes, the documented
      # build silently starts needing a prebuilt library and fails for customers first.
      - name: Confirm the link mode CMake actually used
        shell: bash
        run: |
          mode=$(grep -E '^CAS_BACNET_STACK_LINK:' build/CMakeCache.txt | cut -d= -f2)
          echo "CAS_BACNET_STACK_LINK=$mode"
          [ "$mode" = "SOURCE" ] || { echo "FAIL: expected SOURCE, got '$mode'"; exit 1; }

      - name: Build
        run: cmake --build build --config Release
      # SC-TRANSPORT:BEGIN - Code signing, part 2: sign the executable (Windows, tag builds
      # only; part 1, the Azure login and checks, ran first). Signs before the smoke test
      # and metrics, so the published binary, its metrics and its checksum are all the
      # signed one.
      - name: Sign executable with Azure Artifact Signing
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        uses: Azure/artifact-signing-action@c7ab2a863ab5f9a846ddb8265964877ef296ee82 # v2.0.0
        with:
          endpoint: ${{ vars.AS_ENDPOINT }}
          signing-account-name: ${{ vars.AS_SIGNING_ACCOUNT_NAME }}
          certificate-profile-name: ${{ vars.AS_CERTIFICATE_PROFILE_NAME }}
          files: |
            ${{ github.workspace }}\${{ matrix.exe }}
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256

      - name: Verify signature
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        shell: pwsh
        env:
          EXE_PATH: ${{ github.workspace }}\${{ matrix.exe }}
        run: |
          if (-not (Test-Path -LiteralPath $env:EXE_PATH)) { throw "Executable does not exist: $env:EXE_PATH" }

          $searchRoots = @(
            "${env:ProgramFiles(x86)}\Windows Kits\10\bin",
            "${env:ProgramFiles(x86)}\Windows Kits\11\bin"
          ) | Where-Object { $_ -and (Test-Path -LiteralPath $_) }

          $signtool = $null
          foreach ($root in $searchRoots) {
            $candidate = Get-ChildItem -Path $root -Recurse -Filter signtool.exe -ErrorAction SilentlyContinue |
              Where-Object { $_.FullName -match "\\x64\\signtool\.exe$" } |
              Sort-Object FullName -Descending |
              Select-Object -First 1
            if ($candidate) {
              $signtool = $candidate.FullName
              break
            }
          }

          if (-not $signtool) {
            throw "signtool.exe not found under Windows Kits SDK paths."
          }

          Write-Host "Using signtool: $signtool"
          & $signtool verify /pa /v "$env:EXE_PATH"
          if ($LASTEXITCODE -ne 0) { throw "signtool verify failed ($LASTEXITCODE)" }
      # SC-TRANSPORT:END

      - name: Smoke test + metrics (starts, binds, stays up; measures time to 'ready')
        shell: bash
        run: |
          start=$(date +%s%3N)
          "${{ matrix.exe }}" --port 47821 > smoke.log 2>&1 &
          PID=$!
          ready_ms=""
          for i in $(seq 1 100); do
            if grep -q "ready" smoke.log; then ready_ms=$(( $(date +%s%3N) - start )); break; fi
            sleep 0.1
          done
          sleep 2
          if kill -0 "$PID" 2>/dev/null; then kill "$PID" 2>/dev/null || true; else echo "FAIL: device exited early"; cat smoke.log; exit 1; fi
          [ -n "$ready_ms" ] || { echo "FAIL: no 'ready' line"; cat smoke.log; exit 1; }
          cat smoke.log
          bytes=$(stat -c %s "${{ matrix.exe }}" 2>/dev/null || wc -c < "${{ matrix.exe }}")
          sha=$(sha256sum "${{ matrix.exe }}" | cut -c1-16)
          stackver=$(grep -oE 'CAS BACnet Stack version: [0-9.]+' smoke.log | head -1 | grep -oE '[0-9.]+' || true)
          appver=$(grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' smoke.log | head -1 || true)
          # CMAKE_CXX_COMPILER is not a CMakeCache.txt entry under the multi-config Visual
          # Studio generator (only single-config generators like the Linux Unix Makefiles
          # one cache it directly) - grep finding nothing here is expected on Windows, and
          # with pipefail that makes the pipeline's exit status non-zero, so `|| true` keeps
          # a genuinely missing compiler string from aborting the whole metrics step.
          compiler=$(grep -E '^CMAKE_CXX_COMPILER_VERSION|^CMAKE_CXX_COMPILER:' build/CMakeCache.txt | head -2 | tr '\n' ' ' || true)
          [ -n "$compiler" ] || compiler="$(grep -E '^CMAKE_GENERATOR:' build/CMakeCache.txt | cut -d= -f2 || true) ${{ matrix.os }}"
          cat > "${{ matrix.metrics }}" <<EOF
          {
            "example": "BACnetExampleBSCHUB",
            "app_version": "$appver",
            "os": "${{ matrix.os }}",
            "link_mode": "SOURCE",
            "stack_commit": "${{ steps.stack.outputs.sha }}",
            "stack_version": "$stackver",
            "binary_bytes": $bytes,
            "binary_sha256_prefix": "$sha",
            "startup_ms_to_ready": $ready_ms,
            "compiler": "$compiler",
            "built_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_ref": "${{ github.ref_name }}"
          }
          EOF
          cat "${{ matrix.metrics }}"
          { echo '### metrics (${{ matrix.os }})'; echo '```json'; cat "${{ matrix.metrics }}"; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
      # SC-TRANSPORT:BEGIN - the BACnet/SC transport regression suite. Generates a lab
      # certificate set, starts the example with BACnet/SC enabled, and runs the
      # tests/sc/ verification scripts against it (docs/bacnet-sc-transport-plan.md's
      # V1/V2/V6, the CI-runnable subset - V3/V5, real-peer verification against
      # BACnetSCCli, are manual/local only, no such tool is available on a hosted runner).
      - name: Generate BACnet/SC lab test certificates
        shell: bash
        run: '"${{ matrix.exe }}" --sc-cert-dir certs --generate-certs 1'

      - name: Set up Python (for the BACnet/SC test scripts)
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install BACnet/SC test dependencies
        run: python -m pip install -r tests/sc/requirements.txt

      - name: Client certificate files for Windows tools (.pfx, .cer, YABE config)
        run: python tests/sc/cert_files_test.py --cert-dir certs

      - name: Start the example with BACnet/SC enabled
        shell: bash
        run: |
          "${{ matrix.exe }}" --port 47822 --sc-port 47823 --sc-cert-dir certs > sc-smoke.log 2>&1 &
          echo $! > sc-smoke.pid
          for i in $(seq 1 50); do
            grep -q "listening for WebSocket/TLS connections" sc-smoke.log && break
            sleep 0.2
          done
          grep -q "listening for WebSocket/TLS connections" sc-smoke.log || { echo "FAIL: SC listener never came up"; cat sc-smoke.log; exit 1; }

      - name: BACnet/SC listener test (V1/V2)
        run: python tests/sc/hub_listener_test.py --port 47823 --cert-dir certs

      - name: BACnet/SC File objects test (V6)
        run: python tests/sc/file_object_test.py --target 127.0.0.1 --target-port 47822 --cert-dir certs

      - name: HTTP status page (GET /) shows the version
        shell: bash
        run: |
          version=$(grep -oE 'APP_VERSION = "[0-9.]+"' main.cpp | grep -oE '[0-9.]+')
          curl -sf http://127.0.0.1:8080/ | grep -q "Version $version" || { echo "FAIL: GET / did not show Version $version"; exit 1; }
          curl -sf http://127.0.0.1:8080/health | grep -q '"uptime_seconds"'

      - name: ReadPropertyMultiple (DS-RPM-B)
        run: python tests/sc/rpm_test.py --target 127.0.0.1 --target-port 47822

      - name: HTTP endpoints - upload disabled without http-upload-token
        run: python tests/sc/http_test.py --http-port 8080 --cert-dir certs

      # A second instance with an upload token, a device name, network numbers
      # and a log file (issue #33): the HTTP test exercises upload auth,
      # validation and rate limiting; the log file must hold the start-up lines.
      - name: HTTP upload, device name, network numbers and log file
        shell: bash
        run: |
          printf 'http-upload-token = ci-upload-token\ndevice-name = CI Hub 7\nip-network-number = 11\nsc-network-number = 12\nlog-file = ci-hub.log\nlog-max-size-mb = 1\n' > ci-http.conf
          "${{ matrix.exe }}" --config ci-http.conf --port 47831 --sc-port 47832 --http-port 18084 --sc-cert-dir certs > ci-http.out 2>&1 &
          pid=$!
          for i in $(seq 1 50); do
            curl -sf http://127.0.0.1:18084/health > /dev/null && break
            sleep 0.2
          done
          rc=0
          python tests/sc/http_test.py --http-port 18084 --token ci-upload-token --cert-dir certs --device-name "CI Hub 7" || rc=1
          grep -q "ready" ci-hub.log || { echo "FAIL: ci-hub.log does not have the start-up lines"; rc=1; }
          kill "$pid" 2>/dev/null || true
          [ "$rc" -eq 0 ] || { cat ci-http.out; cat ci-hub.log; exit 1; }

      # The hub CONNECTOR role against tests/sc/fake_hub_server.py (V4).
      - name: BACnet/SC connector against a fake hub
        shell: bash
        run: |
          python -u tests/sc/fake_hub_server.py --port 47833 --cert-dir certs --max-seconds 30 > fake-hub.log 2>&1 &
          fake=$!
          sleep 2
          "${{ matrix.exe }}" --port 47834 --sc-port 47835 --http-port 18085 --sc-cert-dir certs \
            --sc-hub-uri wss://127.0.0.1:47833/ > connector.log 2>&1 &
          pid=$!
          for i in $(seq 1 100); do
            grep -q "Connect-Accept sent" fake-hub.log && grep -q "SC audit: connected to hub" connector.log && break
            sleep 0.2
          done
          rc=0
          grep -q "Connect-Accept sent" fake-hub.log || { echo "FAIL: the fake hub never answered a Connect-Request"; rc=1; }
          grep -q "SC audit: connected to hub" connector.log || { echo "FAIL: the connector never connected"; rc=1; }
          kill "$pid" "$fake" 2>/dev/null || true
          [ "$rc" -eq 0 ] || { cat fake-hub.log; cat connector.log; exit 1; }

      # The clause 19.8.3 certificate procedures over BACnet (add issuer, rejected
      # activation, replace operational certificate). Runs a SECOND instance on its
      # own certificate sets, because the test rewrites the hub's certificates.
      - name: Connection limit - more than 4 BACnet/SC connections is refused
        shell: bash
        run: |
          rc=0
          "${{ matrix.exe }}" --sc-max-hub-connections 5 > limit.log 2>&1 || rc=$?
          cat limit.log
          [ "$rc" -ne 0 ] || { echo "FAIL: --sc-max-hub-connections 5 did not exit with an error"; exit 1; }
          grep -q "support@chipkin.com" limit.log || { echo "FAIL: limit error does not name support@chipkin.com"; exit 1; }

      - name: BACnet/SC only - --bacnet-ip off opens no UDP port, BACnet/SC still works
        shell: bash
        run: |
          "${{ matrix.exe }}" --bacnet-ip off --port 47826 --sc-port 47827 --http-port 18082 --sc-cert-dir certs > sc-only.log 2>&1 &
          pid=$!
          for i in $(seq 1 50); do
            grep -q "listening for WebSocket/TLS connections" sc-only.log && break
            sleep 0.2
          done
          rc=0
          grep -q "BACnet/IP is OFF" sc-only.log || { echo "FAIL: no 'BACnet/IP is OFF' line"; rc=1; }
          # The UDP port must be free: nothing bound it.
          python -c "import socket; s=socket.socket(socket.AF_INET, socket.SOCK_DGRAM); s.bind(('0.0.0.0', 47826)); print('UDP 47826 is free')" || { echo "FAIL: UDP 47826 is bound"; rc=1; }
          curl -sf http://127.0.0.1:18082/health | grep -q '"bacnet_ip_enabled":false' || { echo "FAIL: /health does not report bacnet_ip_enabled false"; rc=1; }
          python tests/sc/hub_listener_test.py --port 47827 --cert-dir certs || rc=1
          kill "$pid" 2>/dev/null || true
          [ "$rc" -eq 0 ] || { cat sc-only.log; exit 1; }

      - name: BACnet/SC certificate procedures over BACnet (clause 19.8.3)
        shell: bash
        run: |
          "${{ matrix.exe }}" --sc-cert-dir ci-hub-certs --generate-certs 1
          "${{ matrix.exe }}" --sc-cert-dir ci-other-certs --generate-certs 1
          "${{ matrix.exe }}" --port 47824 --sc-port 47825 --http-port 18081 --sc-cert-dir ci-hub-certs > cert-proc.log 2>&1 &
          echo $! > cert-proc.pid
          for i in $(seq 1 50); do
            grep -q "listening for WebSocket/TLS connections" cert-proc.log && break
            sleep 0.2
          done
          rc=0
          python tests/sc/cert_procedure_test.py --target-port 47824 --sc-port 47825 \
            --cert-dir ci-hub-certs --second-issuer-dir ci-other-certs || rc=$?
          kill "$(cat cert-proc.pid)" 2>/dev/null || true
          [ "$rc" -eq 0 ] || { cat cert-proc.log; exit "$rc"; }

      - name: Stop the example
        shell: bash
        if: always()
        run: |
          [ -f sc-smoke.pid ] && kill "$(cat sc-smoke.pid)" 2>/dev/null || true
          cat sc-smoke.log || true
      # SC-TRANSPORT:END

      - name: Package
        shell: bash
        run: |
          mkdir -p dist
          cp "${{ matrix.exe }}" "${{ matrix.metrics }}" dist/
          cp README.md TUTORIAL.md LICENSE dist/ 2>/dev/null || true
          cp docs/PICS.md dist/ 2>/dev/null || true
          # SC-TRANSPORT:BEGIN - this example's package also ships the PICS as a PDF and
          # its third-party-dependency notices (libwebsockets, OpenSSL) - no other
          # example in the series has either.
          cp docs/PICS.pdf docs/manual.pdf docs/fact-sheet.pdf dist/ 2>/dev/null || true
          cp THIRD-PARTY-NOTICES.md dist/ 2>/dev/null || true
          cp docs/production-certificates.md dist/ 2>/dev/null || true
          cp SECURITY.md docs/SUPPORT.md dist/ 2>/dev/null || true
          # Software bill of materials (issue #32): this example, the CAS BACnet
          # Stack and every vcpkg library in the executable, with versions and
          # licences. One per platform (the vcpkg libraries differ).
          python tools/make-sbom.py --build-dir build --out "dist/sbom-${{ runner.os == 'Windows' && 'windows' || 'linux' }}.cdx.json"
          cp example.conf dist/
          # SC-TRANSPORT:END

      # SC-TRANSPORT:BEGIN - installers (issue #34)
      # The installer wraps the executable that "Sign executable with Azure Artifact
      # Signing" signed on a tag build; the installer itself is not signed yet.
      - name: Windows installer (Inno Setup)
        id: installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = (Select-String -Path main.cpp -Pattern 'APP_VERSION = "([0-9.]+)"').Matches[0].Groups[1].Value
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) { choco install innosetup -y --no-progress | Out-Null }
          & $iscc /Q "/DAppVersion=$version" "/DSourceDir=$PWD\dist" "/DOutputDir=$PWD\dist" packaging\windows\installer.iss
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          "path=$PWD\dist\BACnetSCHub-$version-setup.exe" >> $env:GITHUB_OUTPUT
          Get-ChildItem dist

      # Sign the installer too (issue #34), with the same Azure Artifact Signing
      # account and profile as the executable, on tag builds only. The Azure login
      # from the start of this job is still valid.
      - name: Sign installer with Azure Artifact Signing
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        uses: Azure/artifact-signing-action@c7ab2a863ab5f9a846ddb8265964877ef296ee82 # v2.0.0
        with:
          endpoint: ${{ vars.AS_ENDPOINT }}
          signing-account-name: ${{ vars.AS_SIGNING_ACCOUNT_NAME }}
          certificate-profile-name: ${{ vars.AS_CERTIFICATE_PROFILE_NAME }}
          files: |
            ${{ steps.installer.outputs.path }}
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256

      - name: Verify installer signature
        if: startsWith(github.ref, 'refs/tags/v') && runner.os == 'Windows'
        shell: pwsh
        run: |
          $setup = Get-ChildItem dist -Filter "*-setup.exe" | Select-Object -First 1
          if (-not $setup) { throw "No installer in dist" }
          $sig = Get-AuthenticodeSignature -LiteralPath $setup.FullName
          Write-Host "$($setup.Name): $($sig.Status) - $($sig.SignerCertificate.Subject)"
          if ($sig.Status -ne 'Valid') { throw "The installer is not validly signed ($($sig.Status))" }

      - name: Linux archive and .deb package
        if: runner.os == 'Linux'
        shell: bash
        run: |
          version=$(grep -oE 'APP_VERSION = "[0-9.]+"' main.cpp | grep -oE '[0-9.]+')
          stage="BACnetSCHub-$version-linux-x64"
          mkdir -p "$stage"
          cp dist/BACnetExampleBSCHUB dist/*.md dist/*.pdf dist/LICENSE dist/example.conf "$stage/" 2>/dev/null || true
          cp packaging/linux/install.sh packaging/linux/uninstall.sh packaging/linux/bacnet-schub-hub.service "$stage/"
          chmod +x "$stage/install.sh" "$stage/uninstall.sh" "$stage/BACnetExampleBSCHUB"
          tar -czf "dist/$stage.tar.gz" "$stage"
          sh packaging/linux/build-deb.sh "$version" "$stage" dist
          dpkg-deb --info "dist/bacnet-schub-hub_${version}_amd64.deb"
          ls -l dist

      # Install the packages on the runner and check the service really runs
      # (issue #34): the runners have admin rights and, on Linux, systemd.
      - name: Test the .deb - install, service up, /health, purge
        if: runner.os == 'Linux'
        shell: bash
        run: |
          version=$(grep -oE 'APP_VERSION = "[0-9.]+"' main.cpp | grep -oE '[0-9.]+')
          sudo apt-get install -y -qq "./dist/bacnet-schub-hub_${version}_amd64.deb" > /dev/null
          ok=0
          for i in $(seq 1 30); do
            curl -sf http://127.0.0.1:8080/health > /dev/null && { ok=1; break; }
            sleep 1
          done
          systemctl is-active bacnet-schub-hub
          sudo journalctl -u bacnet-schub-hub --no-pager | tail -20
          [ "$ok" = 1 ] || { echo "FAIL: the bacnet-schub-hub service did not answer /health"; exit 1; }
          curl -s http://127.0.0.1:8080/health; echo
          test -f /var/log/bacnet-schub/hub.log || { echo "FAIL: no /var/log/bacnet-schub/hub.log"; exit 1; }
          sudo apt-get purge -y -qq bacnet-schub-hub > /dev/null
          ! systemctl is-active bacnet-schub-hub

      - name: Test the Windows installer - silent install, service up, /health, uninstall
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $setup = Get-ChildItem dist -Filter "*-setup.exe" | Select-Object -First 1
          Start-Process -FilePath $setup.FullName -ArgumentList '/VERYSILENT', '/SUPPRESSMSGBOXES', '/NORESTART' -Wait
          $ok = $false
          foreach ($i in 1..30) {
            try { Invoke-RestMethod http://127.0.0.1:8080/health | ConvertTo-Json -Compress; $ok = $true; break } catch { Start-Sleep 1 }
          }
          Get-Service BACnetSCHub | Format-List Name, Status, StartType
          Get-Content "$env:ProgramData\Chipkin\BACnetSCHub\logs\hub.log" -Tail 20 -ErrorAction SilentlyContinue
          if (-not $ok) { throw "The BACnetSCHub service did not answer /health" }
          if ((Get-Service BACnetSCHub).StartType -ne 'Automatic') { throw "The service does not start automatically" }
          $uninstaller = Get-ChildItem "${env:ProgramFiles}\Chipkin\BACnet SC Hub" -Filter "unins*.exe" | Select-Object -First 1
          Start-Process -FilePath $uninstaller.FullName -ArgumentList '/VERYSILENT', '/SUPPRESSMSGBOXES' -Wait
          if (Get-Service BACnetSCHub -ErrorAction SilentlyContinue) { throw "Uninstall left the BACnetSCHub service behind" }
      # SC-TRANSPORT:END

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/*

  release:
    name: publish release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write      # publish the release
      id-token: write      # build-provenance attestations (Sigstore, keyless)
      attestations: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      # Every platform artifact carries the same docs (README.md, LICENSE, ...).
      # Uploading two assets with one name makes the release action fail part-way
      # and leaves a draft release behind (a real tagged release hit this). Flatten the
      # artifacts into one folder with a single copy of each file name, and stop
      # if two files share a name but differ.
      - name: Collect release assets (one copy of each file name)
        shell: bash
        run: |
          mkdir -p release
          find artifacts -type f | sort | while read -r f; do
            name=$(basename "$f")
            if [ -e "release/$name" ]; then
              cmp -s "$f" "release/$name" || { echo "FAIL: two different files are both named $name ($f)"; exit 1; }
            else
              cp "$f" "release/$name"
            fi
          done
          ls -l release
      # SC-TRANSPORT:BEGIN - SHA-256 checksums of every release asset, so a download can be
      # checked (sha256sum -c SHA256SUMS.txt, or Get-FileHash on Windows).
      - name: Checksums
        shell: bash
        run: |
          cd release
          sha256sum * > SHA256SUMS.txt
          cat SHA256SUMS.txt

      # Signed build provenance for every release asset (issue #34): the Linux
      # archive and .deb as well as the Windows files. Keyless - GitHub OIDC and
      # Sigstore, no key to store. Anyone can check a download came from this
      # workflow and commit with: gh attestation verify <file> --repo <owner>/<repo>
      - name: Attest build provenance
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: release/*
      # SC-TRANSPORT:END
      - uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: release/*
